---
title: "Data Quality"
author: "Tobsen1x"
date: "Monday, November 24, 2014"
output: html_document
---

```{r echo=FALSE, prompt=FALSE}
source('loadData.R', encoding = 'UTF8')
data <- loadData('BL1')
```

## Abstract
This report bases on the data loaded from loadData.R.
It evaluates the data quality to identify possible bugs in the HTML parser, DB layer
or inconstancies on the homepages.

### TransPos

```{r}
table(data$transPos, useNA = 'always')
```

The NAs should only contain in BENCH Einsätzen (verify in Parser!!!).
Take a look at these observations:

```{r}
spielerStats[is.na(spielerStats$transPos) & !(spielerStats$einsatz %in% c('BENCH')), ]
```

### TransName

Why do observations without a transName exist?

```{r}
sum(is.na(spielerStats$transName)) / length(spielerStats$transName)
```


